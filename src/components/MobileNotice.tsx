export function MobileNotice() {
  return (
    <div className="fixed inset-x-0 top-0 z-50 border-b border-node-border bg-node px-4 py-2 text-center font-mono text-xs text-text-secondary md:hidden">
      Codebase Cartographer is designed for larger screens — try this on a
      desktop or tablet for the full experience.
    </div>
  );
}
